<!doctype html>
<html>
<head>
    <% include ../../Components/settings.html %>
    <script src="/echarts/echarts.min.js"></script>
    <title>BUFF-NN</title>
</head>
<body>
<!-- Main container -->
<div class="page-container">
    <% include ../../Components/navigator.ejs %>

    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="box">
                    <div class="box-icon">
                        <span class="fa fa-4x fa-lightbulb-o"></span>
                    </div>
                    <div class="info text-center">
                        <h4 class="text-center">NN机器学习</h4>
                        <p>嗨, WAN！您在2017年6月10日对个股——平安银行（000001）的BUFF-NN分析结果已经得出，以下给出该分析方法分析出的各种变化情况的概率。
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row" style="margin-top: 20px">
        <div id="nn_pie_chart" class="col-md-12" style="height: 500px"></div>
    </div>

    <script>
        let nn_pie_chart = echarts.init(document.getElementById('nn_pie_chart'));

        let pie_data = [];

        eval('pie_data = <%= JSON.stringify(pie_data) %>;');

        let nn_pie_option = {
            title: {
                text: '涨跌幅概率分布图',
                show: true,
                left: 'center'
            },
            legend: {
                x: 'center',
                y: 'bottom',
                data: ['小于 -10%', '-10% ~ -5%', '-5% ~ 0', '0 ~ 5%', '5% ~ 10%', '大于 10%']
            },
            tooltip: {
                trigger: 'item',
                formatter: "{b}<br/> 概率 : {d}%"
            },
            series: [
                {
                    name: '概率分布',
                    type: 'pie',
                    radius: '70%',
                    center: ['50%', '50%'],
                    data: [
                        {value: pie_data[0], name: '小于 -10%'},
                        {value: pie_data[1], name: '-10% ~ -5%'},
                        {value: pie_data[2], name: '-5% ~ 0'},
                        {value: pie_data[3], name: '0 ~ 5%'},
                        {value: pie_data[4], name: '5% ~ 10%'},
                        {value: pie_data[5], name: '大于 10%'}
                    ],
                    animationType: 'scale',
                    animationEasing: 'elasticOut',
                    animationDelay: function (idx) {
                        return Math.random() * 200;
                    }
                }
            ]
        };

        nn_pie_chart.setOption(nn_pie_option);

        setTimeout(() => {
            window.onresize = function () {
                nn_pie_chart.resize();
            }
        }, 200);
    </script>

    <!-- ScrollToTop Button -->
    <a class="bloc-button btn btn-d scrollToTop" onclick="scrollToTarget('1')"><span
                class="fa fa-chevron-up"></span></a>
    <!-- ScrollToTop Button END-->

</div>
<!-- Main container END -->
</body>

<% include ../../Components/footer.ejs %>
</html>
